File Manager (with hella options) - Woop woop IDK who would actually make a CLI like this

USAGE

    fm [option]
    fm [subcommand]

SUBCOMMANDS

    read
        Read a file.

OPTIONS

    -h, --help
        Show this menu.
    -v, --version
        Show this CLI's version.

    -L, --log
        Log a value.